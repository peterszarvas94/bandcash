package entry

import shared "bandcash/models/shared"

templ EntryShow(data EntryData) {
	<!doctype html>
	<html lang="en">
		@shared.Head(data.Title)
		<body>
			<div data-signals={templ.JSONString(entryShowSignals(data))} data-init="@get('/sse')"></div>
			@shared.TwoCol("entry-show", EntryShowMain(data), EntryShowSidebar(data))
		</body>
	</html>
}
