package home

import (
	shared "bandcash/models/shared"
)

templ HomeIndex(data HomeData) {
	@shared.HomeLayout(
		"app",
		data.Title,
		nil,
		data.Breadcrumbs,
		data.UserEmail,
		data.IsAuthenticated,
		HomeIndexMain(data),
	)
}
